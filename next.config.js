/** @type {import('next').NextConfig} */
const nextConfig = {
    
    compiler: {
        styledComponents: true,
    },
};

module.exports = {
    pageExtensions: ["jsx", "js", "ts", "tsx"],
    nextConfig
} 
